# Vite React Example

Minimal consumer app for `@arthav/open-loading` in Vite.

```bash
npm install
npm run dev
```

The example imports `@arthav/open-loading/styles.css` once in `src/main.tsx`, then renders package components directly in `src/App.tsx`.
