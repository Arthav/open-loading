# Next.js App Router Example

Minimal App Router consumer app for `@arthav/open-loading`.

```bash
npm install
npm run dev
```

The stylesheet is imported once from `app/layout.tsx`. Loader components render from a client component because App Router server components should not own interactive loading UI.
